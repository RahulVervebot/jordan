/** @type {import('next').NextConfig} */
const nextConfig = {
    reactStrictMode: true,
     images: {
        domains: ['jordan.agoxn.cloud'],
      },
    
}

module.exports = nextConfig
